# Audit Arena

Command:

- `settler arena run <scenario.json> --strategies strict,balanced`

Produces reproducible strategy scorecards and writes a JSON artifact for deterministic comparison across correctness, determinism, audit completeness, and latency.
