# OSS Registry

Marketplace commands:

- `settler adapters search|install|verify`
- `settler rules search|install|verify`

Registry entries require license, provenance, and compatibility metadata. Install/verify commands enforce metadata presence for compliance-safe adoption.
