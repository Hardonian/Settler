# Flow Explorer

CLI export:

- `settler flow export <runId|path> --format json`
- `settler flow export <runId|path> --format svg`

The artifact includes stage progression (ingest → normalize → match → settle → export), audit overlays, and export verification state.
