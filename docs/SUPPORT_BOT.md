# Support Bot

Command:

- `settler support ask "<question>"`

The support bot is offline-first using `support/kb_index.json`, deterministic keyword retrieval, and output redaction for token-like strings.
