# First-run demo

Run:

```bash
pnpm run demo
```

Expected output includes:

- `Run Fingerprint: <hash>`
- `Replay Verified: OK`

This verifies deterministic run + replay behavior without requiring optional integrations.
