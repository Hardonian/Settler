# Tenant Lineage

CLI export:

- `settler lineage export --tenant <id> --format json`
- `settler lineage export --tenant <id> --format svg`

Lineage output is tenant-scoped and summarizes source/transform/output touch points from run artifacts.
