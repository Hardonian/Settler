# Governance Templates

Commands:

- `settler init adapter --governed --name <name>`
- `settler init rule --governed --name <name>`

Generated scaffolds include deterministic replay/conformance JSON fixtures and docs stubs with schema pins.
