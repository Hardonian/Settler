# Phase 2 Readiness Plan

This repository now captures governance-ready data signals:

- policy id/hash in run metrics
- deny/budget-overrun event table (`policy_metrics`)
- replay verification mismatches (`drift_metrics`)
- cost units (`economic_metrics`)

Planned next steps:

1. approval workflow UI/API
2. policy diff and simulation UX
3. budget controls with enforcement and notifications
4. drift alert routing and incident integration
