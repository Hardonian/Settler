{
  "name": "@settler/agents",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "security:scan": "node dist/security-agent.js --scan=all"
  },
  "dependencies": {
    "@settler/logger": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.3.3"
  }
}
