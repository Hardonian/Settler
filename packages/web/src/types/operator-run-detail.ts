/**
 * Canonical operator run detail DTO from `@settler/reconciliation-core`.
 * Re-export keeps console consumers aligned with the core contract without duplicating shapes.
 */
export type {
  OperatorRunDetail,
  OperatorRunStageRow,
} from "@settler/reconciliation-core";
