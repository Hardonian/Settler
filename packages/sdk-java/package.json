{
  "name": "@settler/sdk-java",
  "version": "0.1.0",
  "private": true,
  "description": "Settler Java SDK workspace metadata",
  "scripts": {
    "verify:presence": "echo 'java sdk metadata workspace'",
    "test": "node -e \"try { require('child_process').execSync('mvn -v', {stdio:'ignore'}); require('child_process').execSync('mvn test -pl packages/sdk-java', {stdio:'inherit'}); } catch { console.log('Skipping Java SDK tests: Maven (mvn) not found in environment'); }\""
  },
  "dependencies": {}
}
