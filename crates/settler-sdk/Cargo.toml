[package]
name = "settler-sdk"
version = "0.1.0"
edition.workspace = true
license.workspace = true
repository.workspace = true
description = "Astra Rust Kernel SDK Bindings for low-latency embedded reconciliation proxies"

[dependencies]
settler-kernel = { path = "../settler-kernel" }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
sha2 = "0.11"
hex = "0.4"

[dev-dependencies]
pretty_assertions = "1.4"
