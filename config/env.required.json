{
  "version": 1,
  "updatedAt": "2026-03-13",
  "variables": [
    {
      "name": "NEXT_PUBLIC_SUPABASE_URL",
      "scope": ["development", "preview", "production"],
      "surface": "public",
      "required": true,
      "aliases": ["SUPABASE_URL"],
      "notes": "Canonical public Supabase URL for Next.js client/runtime."
    },
    {
      "name": "NEXT_PUBLIC_SUPABASE_ANON_KEY",
      "scope": ["development", "preview", "production"],
      "surface": "public",
      "required": true,
      "aliases": ["SUPABASE_ANON_KEY"],
      "notes": "Canonical public anon key for browser SDK."
    },
    {
      "name": "SUPABASE_SERVICE_ROLE_KEY",
      "scope": ["development", "preview", "production"],
      "surface": "server",
      "required": true,
      "aliases": [],
      "notes": "Server-only admin key; never expose in client bundles."
    },
    {
      "name": "DATABASE_URL",
      "scope": ["development", "preview", "production"],
      "surface": "server",
      "required": true,
      "aliases": ["SUPABASE_DATABASE_URL", "DIRECT_URL", "SUPABASE_DB_URL"],
      "notes": "Canonical DB connection URL used by Prisma and migration tooling. SUPABASE_DB_URL remains legacy compatibility alias for scripts/workflows."
    },
    {
      "name": "SUPABASE_URL",
      "scope": ["development", "preview", "production"],
      "surface": "server",
      "required": false,
      "aliases": ["NEXT_PUBLIC_SUPABASE_URL"],
      "notes": "Server alias preserved for backward compatibility."
    },
    {
      "name": "SUPABASE_ANON_KEY",
      "scope": ["development", "preview", "production"],
      "surface": "server",
      "required": false,
      "aliases": ["NEXT_PUBLIC_SUPABASE_ANON_KEY"],
      "notes": "Server alias preserved for backward compatibility."
    },
    {
      "name": "VERCEL_ENV",
      "scope": ["preview", "production"],
      "surface": "server",
      "required": false,
      "aliases": [],
      "notes": "Deployment context in Vercel runtime."
    },
    {
      "name": "VERCEL_URL",
      "scope": ["preview", "production"],
      "surface": "server",
      "required": false,
      "aliases": [],
      "notes": "Host URL in Vercel runtime."
    },
    {
      "name": "DOPPLER_TOKEN",
      "scope": ["development", "preview", "production"],
      "surface": "server",
      "required": false,
      "aliases": [],
      "notes": "Only required when using Doppler CLI/token injection workflows."
    },
    {
      "name": "SUPABASE_DB_URL",
      "scope": ["development", "preview", "production"],
      "surface": "server",
      "required": false,
      "aliases": ["DATABASE_URL"],
      "notes": "Legacy database URL alias retained for older scripts/workflows."
    }
  ],
  "requirements": {
    "groups": [
      {
        "label": "supabase-url",
        "required": true,
        "keys": ["NEXT_PUBLIC_SUPABASE_URL", "SUPABASE_URL"]
      },
      {
        "label": "supabase-anon",
        "required": true,
        "keys": ["NEXT_PUBLIC_SUPABASE_ANON_KEY", "SUPABASE_ANON_KEY"]
      },
      {
        "label": "database-url",
        "required": true,
        "keys": ["DATABASE_URL", "SUPABASE_DATABASE_URL", "DIRECT_URL", "SUPABASE_DB_URL"]
      },
      {
        "label": "service-role",
        "required": true,
        "keys": ["SUPABASE_SERVICE_ROLE_KEY"]
      }
    ]
  }
}
