💡 **What:** Replaced Node 20 runner environments with Node 24 by using \`ACTIONS_ALLOW_USE_UNSECURE_NODE_VERSION=true\` for older workflows. Fixed CodeQL, setup-node, and gitleaks actions version. Fixed \`cargo-deny\` action by replacing it with a native \`cargo install\`. Added \`noEmit: false\` to logger \`tsconfig.json\` to ensure \`dist\` output is produced for the agents build. And added \`GITHUB_TOKEN\` to the \`gitleaks\` workflow.
