# Generators

Canonical generator implementation lives in:

- `packages/cli/src/lib/reconciliation-foundry.ts`

CLI entrypoints:

- `foundry reconciliation-generate`
- `foundry reconciliation-verify`
