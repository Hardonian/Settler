/** Minimal Next.js config placeholder for CI surface reproduction */
/** This patch intentionally keeps config minimal to avoid environment-specific issues */
module.exports = {
  ReactStrictMode: false,
  swcMinify: true,
};
